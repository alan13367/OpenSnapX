// Keep portable fallback signing in source control. Developers can override it
// without changing project.yml by creating Signing.local.xcconfig beside this file.
CODE_SIGN_STYLE = Automatic
CODE_SIGN_IDENTITY = -
DEVELOPMENT_TEAM =

#include? "Signing.local.xcconfig"
